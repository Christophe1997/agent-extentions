{
  "description": "yapermission PreToolUse policy gate — evaluates every tool call against the active TOML ruleset",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/yapermission.py hook",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
